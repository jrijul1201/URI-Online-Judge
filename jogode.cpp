#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    while (cin >> t)
    {
        int a[t][3];
        char x;
        for (int i = 0; i < 3; i++)
        {
            cin >> a[i][0] >> a[i][1] >> x >> a[i][2];
        }
        string p[t];
        int pi[t];
        char po[t];
        for (int i = 0; i < 3; i++)
        {
            cin >> p[i];
            cin >> pi[i] >> po[i];
        }
        
    }

    return 0;
}