#include <bits/stdc++.h>
using namespace std;
int main()
{
    float a, b, c, avg;
    cin >> a >> b >> c;
    avg = ((2 * a) + (3 * b) + (5 * c ))/10;
    cout<<"MEDIA = "<<fixed<<setprecision(1)<<avg<<"\n";
    return 0;
}
