#include <bits/stdc++.h>
using namespace std;
int main()
{
    double n;
    while (cin >> n)
    {
        if (n >= 0.5)
            cout << fixed << setprecision(0) << n << "\n";
    }
    return 0;
}