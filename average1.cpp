#include <bits/stdc++.h>
using namespace std;
int main()
{
    float a, b, avg;
    cin >> a >> b;
    avg = (3.5 * a + 7.5 * b) / 11;
    cout << "MEDIA = " << fixed << setprecision(5) << avg << "\n";
    return 0;
}
