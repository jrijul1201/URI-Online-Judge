#include <bits/stdc++.h>
using namespace std;
int main()
{
    int t = 1;
    while (true)
    {
        cin >> t;
        if (t == 0)
            break;
        char s[t];
        for(int i = 0; i < t; i++)
        {
           cin>>s[i];
        }
    }
    return 0;
}