// gets() : Works on character array. eg char string[LENGTH]. Syntax: gets(string)
// getline(): Works on string datatype. eg. string S. Sytax: getline(cin,S)
// scanf("%s",A) will work if A is declared as char A[LENGTH]. ie. A holds address.
// scanf("%s",&A) will not get the job done as to hold string you need character array or a string variable where in you dont use '&' operator. A already holds address.
// scanf(%s\n",A): you need to again enter some string after you've entered the string you want in A.

for
    extra space in getline,
        use getchar();